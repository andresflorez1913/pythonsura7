# varible con elementos del mismo tipo
# lista numeros pares
numerosPares=[]
contador=0
while(contador<10):
    numero=(int (input("Digite un numero par ")))
    if(numeros % 2==0):
      numerosPares.append()
      contador=contador+1
    
for observador in numerosPares:
        print(observador)